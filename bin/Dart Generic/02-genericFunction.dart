import 'data/02-array_helper.dart';

void main() {
  var number = [1, 2, 3, 4, 5, 6];
  var names = ["Yapet", "Lukita", "Warm"];

  print(ArrayHelper.count(number));
  print(ArrayHelper.count(names));
}
