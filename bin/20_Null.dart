void main() {
// Kata kunci null merupakan tipe data representasi data kosong
// Secara default, variable harus diisi, jika tidak, maka variable tidak bisa digunakan
/* Membuat variable yang diisi dengan data null, tambahkan ? (tanda tanya), seperti :
  TipeData? namaVariable  = null;
*/

  // int number1; // Error!
  int? number2;

  // print(number1); // Error!
  print(number2);
}
